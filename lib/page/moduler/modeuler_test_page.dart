import 'package:flutter/material.dart';

class ModulerTestPage extends StatelessWidget {
  final String name;

  ModulerTestPage(this.name);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
